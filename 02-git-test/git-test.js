// Instructions: Replace the empty strings with the correct Git commands.

const answers = {
  clone: '', // Command to clone the repository 'https://github.com/example/repo.git'
  createBranch: '', // Command to create and switch to a new branch named 'my-new-feature'
  stageChanges: '', // Command to stage all changes in the current directory
  commitChanges: '', // Command to commit staged changes with the message "Add new feature"
  pushChanges: '', // Command to push the 'my-new-feature' branch to the remote 'origin'
  checkStatus: '', // Command to check the status of your working directory and staging area
  pullChanges: '', // Command to fetch and merge changes from the remote 'origin' into your current branch
  mergeBranch: '', // Command to merge the 'my-new-feature' branch into the 'main' branch
  listBranches: '', // Command to list all local branches
};

module.exports = answers;
