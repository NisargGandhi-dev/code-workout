// This file demonstrates higher-order functions.

function mapNumbersToSquared(numbers) {
  // Given an array of numbers, return a new array where each number is squared.
}

function filterObjectsByType(objects, type) {
  // Given an array of objects (e.g., [{ id: 1, type: 'A' }]),
  // return a new array containing only the objects with the specified 'type'.
}

function reduceNumbersToSum(numbers) {
  // Given an array of numbers, calculate and return their sum.
}

module.exports = {
  mapNumbersToSquared,
  filterObjectsByType,
  reduceNumbersToSum,
};