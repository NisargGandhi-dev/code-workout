// This function demonstrates array types in TypeScript.
function arrayTypesExample(numbers: number[], strings: string[]): string {
  // Return a string that includes all the elements of both arrays.
  // e.g., "Numbers: 1, 2, 3, Strings: a, b, c"
}

export default arrayTypesExample;
