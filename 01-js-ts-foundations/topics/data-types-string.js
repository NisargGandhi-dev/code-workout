// This function demonstrates the use of strings.
function stringExample() {
  // Create two string variables, 'greeting' and 'noun'.
  // Concatenate them to form the string "Hello, World!" and return it.
}

module.exports = stringExample;