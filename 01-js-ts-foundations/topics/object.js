// This function demonstrates the use of objects.
function objectExample() {
  // Create an object with properties 'name' (string) and 'age' (number).
  // Add a new property 'city' (string) to the object.
  // Return the modified object.
}

module.exports = objectExample;