# Cypress Command Test

This is a hands-on test to check your knowledge of fundamental Cypress commands.

## The Challenge

Your task is to implement the Cypress commands in the `topics/` directory. Each file in `topics/` contains a comment and a description of what needs to be implemented. **Important:** You should run the test file only once the file has been completed and you are not allowed to check logs of tests. Checking test logs will directly reveal the answers. Once you've implemented a command, run its corresponding test file in `tests/` to verify your solution.

## How to Run the Test

First, make sure you have the dependencies installed. If you don't see a `node_modules` folder in this directory, run `npm install`:

```bash
cd 03-cypress-test
npm install
```

Then, run the test command:

```bash
npm test
```

The test output will tell you which of your answers are correct and which are incorrect. Good luck!
